# Architecture

> **Generated file — do not hand-edit.** Run `/keel-map` to regenerate it from the code.
>
> Hand-maintained architecture docs rot, because updating them is a separate act of discipline
> from changing the code, and the discipline always loses. Generating it means the doc is never
> premature and never stale — it describes what is actually there, on the day you asked.

---

_Not yet generated. Run `/keel-map` once there is code to describe._

<!--
/keel-map fills this with:

  - A one-paragraph summary of what the system does
  - The module map: each top-level unit, its responsibility, and what it depends on
  - Data flow for the two or three most important paths
  - The seams — where the system is designed to be replaceable
  - Anything load-bearing that is not obvious from reading the code

At tier L3 it also diffs the generated map against the previous version and reports drift, so
a structural change that nobody mentioned shows up as a finding rather than a surprise.
-->
